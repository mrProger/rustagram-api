datasource db {
    provider = "sqlite"
    url = "file:./database/data.db"
}

generator client {
    provider = "prisma-client-js"
}

model Account {
    id          Int     @id @default(autoincrement())
    login       String  @unique
    password    String

    @@map("account")
}